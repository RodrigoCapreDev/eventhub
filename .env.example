# Este archivo es un ejemplo de configuración de variables de entorno para un proyecto Django.

# Clave secreta de Django (No compartir en producción)
SECRET_KEY=clave-secreta-aca

# Modo debug: True para desarrollo, False para producción
DEBUG=True

# Hosts permitidos para ejecutar la app (separados por coma)
ALLOWED_HOSTS=localhost,127.0.0.1

# --- Base de datos SQLite (para desarrollo local y Docker) ---
# Para SQLite no hace falta configurar nombre de usuario, contraseña ni host.
DATABASE_URL=sqlite:///db.sqlite3